name: Deploy Minecraft Server

on:
  pull_request:
    types: [closed]
    branches: [ "main" ]
    paths:
      - 'helm/**'
      - 'docker/**'
      - 'Dockerfile'
      - '.github/workflows/**'

env:
  # Define aquí el nombre de tu imagen en Docker Hub
  IMAGE_NAME: nicolasestevez/minecraft-survival 

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: Deploy
    # Solo ejecutar si el PR fue mergeado (no solo cerrado)
    if: github.event.pull_request.merged == true
    
    steps:
      - name: 📥 Bajar código del repositorio
        uses: actions/checkout@v4

      - name: 🔐 Login en Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🏷️ Generar Tag de Versión
        id: meta
        run: |
          # Creamos una variable VERSION = v1.0.numero_de_ejecucion
          echo "VERSION=v1.0.${{ github.run_number }}" >> $GITHUB_ENV
          echo "La nueva versión será: v1.0.${{ github.run_number }}"

      - name: 🐳 Construir y Subir Imagen (Build & Push)
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Subimos dos tags: el específico (v1.0.55) y el latest
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.VERSION }}
            ${{ env.IMAGE_NAME }}:latest

      - name: ☸️ Instalar Helm
        uses: azure/setup-helm@v3

      - name: 🔌 Configurar Kubectl
        uses: azure/k8s-set-context@v3
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      - name: 🚀 Desplegar en Kubernetes
        run: |
          echo "Desplegando versión: ${{ env.VERSION }}"
          
          # Registramos el repo de itzg
          helm repo add itzg https://itzg.github.io/minecraft-server-charts/
          helm repo update

          # Creamos el secreto para bajar la imagen (si es privada)
          # Usamos la URL v1 que es compatible con k3s y containerd
          kubectl create secret docker-registry regcred \
            --docker-server=https://index.docker.io/v1/ \
            --docker-username="${{ secrets.DOCKER_USERNAME }}" \
            --docker-password="${{ secrets.DOCKER_PASSWORD }}" \
            --namespace minecraft \
            --dry-run=client -o yaml | kubectl apply -f -

          # Verificación rápida del secreto (obfuscada)
          echo "Verificando existencia del secreto regcred..."
          kubectl get secret regcred -n minecraft

          # ==================================================================================
          # AVISO EN EL JUEGO + GUARDADO (Pre-Deploy)
          # Intentamos mandar un mensaje al server actual si está vivo
          # ==================================================================================
          echo "📢 Notificando a los jugadores y guardando..."
          # Buscamos el pod actual (si existe)
          POD_NAME=$(kubectl get pods -n minecraft -l app=survival-server-minecraft -o jsonpath="{.items[0].metadata.name}" || echo "")
          
          if [ ! -z "$POD_NAME" ]; then
            echo "Pod encontrado: $POD_NAME"
            # Mandamos alerta RCON
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "title @a title {\"text\":\"⚠ ACTUALIZACIÓN ⚠\",\"color\":\"red\"}" || true
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "title @a subtitle {\"text\":\"Reinicio en 30s - Guardando datos...\",\"color\":\"yellow\"}" || true
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "say §c⚠ SERVIDOR REINICIANDO PARA ACTUALIZACIÓN ⚠" || true
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "say §eGuardando mundo..." || true
            
            # Forzamos guardado
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "save-all" || true
            
            # BACKUP DE EMERGENCIA
            echo "📦 Generando backup de emergencia (world)..."
            kubectl exec -n minecraft "$POD_NAME" -- rcon-cli "say §7[System] Creando copia de seguridad..." || true
            # Usamos tar para empaquetar el mundo. Puede tardar unos segundos.
            # Asumimos que el mundo se llama 'world'. Si usas otro nombre, ajustalo.
            kubectl exec -n minecraft "$POD_NAME" -- /bin/bash -c "mkdir -p /data/Backups && tar -czf /data/Backups/pre-deploy-$(date +%Y%m%d-%H%M%S).tar.gz /data/world" || echo "⚠ Aviso: El backup falló o tardó demasiado, pero continuamos."

            # Esperamos un poco para que lean el mensaje
            sleep 5
          else
            echo "No se encontró un pod activo. Saltando notificación ingame."
          fi

          # Actualizamos Helm inyectando la imagen exacta que acabamos de crear
          helm upgrade --install survival-server itzg/minecraft \
            --version 4.19.0 \
            --namespace minecraft \
            -f helm/minecraft-values.yaml \
            --set image.repository=${{ env.IMAGE_NAME }} \
            --set image.tag=${{ env.VERSION }}

          # WORKAROUND: El chart de itzg no usa imagePullSecrets del values.yaml
          # Inyectamos el secreto manualmente en el Deployment
          echo "Inyectando imagePullSecrets en el Deployment..."
          kubectl patch deployment survival-server-minecraft -n minecraft -p '{"spec":{"template":{"spec":{"imagePullSecrets":[{"name":"regcred"}]}}}}'

          # Aplicamos el CronJob de reinicio automático
          echo "Aplicando CronJob de reinicio..."
          kubectl apply -f docker/scripts/minecraft-restart.yaml

          # Forzamos el reinicio para asegurar que baje la nueva imagen
          kubectl rollout restart deployment/survival-server-minecraft -n minecraft
          
          # Esperamos a que el despliegue se complete (timeout 10min)
          echo "Esperando a que el nuevo pod arranque..."
          kubectl rollout status deployment/survival-server-minecraft -n minecraft --timeout=10m

      - name: 🔔 Notificar Éxito a Discord
        uses: rjstone/discord-webhook-notify@v1
        if: success()
        with:
          severity: info
          username: "Minecraft Server"
          details: "🚀 **¡Servidor Actualizado!** (Versión: `${{ env.VERSION }}`)"
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL }}

      - name: 🚨 Notificar Fallo a Discord
        uses: rjstone/discord-webhook-notify@v1
        if: failure()
        with:
          severity: error
          username: "GitOps Deployer"
          details: |
            ❌ **Despliegue FALLIDO**
            Versión Intentada: `${{ env.VERSION }}`
            Verifica los logs en GitHub Actions.
            [Ver Error](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK_URL_PRIVATE }}
